using System;

/*class Student
{
    private string name;
    private int marks;

    // 1. Read-Write Property
    public string Name
    {
        get { return name; }
        set { name = value; }
    }

    // 2. Write-Only Property
    public int Marks
    {
        set { marks = value; }
    }

    // 3. Read-Only Property
    public string Grade
    {
        get
        {
            if (marks >= 90) return "A";
            else if (marks >= 75) return "B";
            else if (marks >= 50) return "C";
            else return "F";
        }
    }

    // 4. Auto-Implemented Property
    public string Email { get; set; }

    // Display method
    public void ShowInfo()
    {
        Console.WriteLine("\nStudent Info:");
        Console.WriteLine("Name: " + Name);
        Console.WriteLine("Email: " + Email);
        Console.WriteLine("Grade: " + Grade);
    }
}

class Program
{
    static void Main()
    {
        Student s = new Student();

        // Using read-write and write-only properties
        s.Name = "Amylee";
        s.Marks = 82;  // Write-only
        s.Email = "amylee@.com";  // Auto property

        // Display info including read-only Grade
        s.ShowInfo();
        Console.ReadKey();
    }
}*/

