using System;


namespace AmillieyPakhrin1
{
    /*
      class Q8
      {
          static void Main()
          {
              Console.Write("Enter the string to be reversed: ");
              string input = Console.ReadLine();

              //change the string to char array
              char[] charArray = input.ToCharArray();

              //reverse the char array
              Array.Reverse(charArray);

              //convert the char array back to string
              string reversedString = new string(charArray);

              //print the reversed string
              Console.WriteLine("The reversed string is:" +reversedString);

              Console.ReadKey();
              /*
              char[] charArray = input.ToCharArray();
              Converts the string into a character array because arrays can be easily reversed.

              For example, "hello" becomes['h', 'e', 'l', 'l', 'o'].

              Array.Reverse(charArray);
              Reverses the character array in place.

              So['h', 'e', 'l', 'l', 'o'] becomes['o', 'l', 'l', 'e', 'h'].

              string reversed = new string(charArray);
              Converts the reversed character array back into a string.

              Now reversed holds "olleh".

          }
      }*/
}
