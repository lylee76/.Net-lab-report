using System;


namespace AmillieyPakhrin1
{/*
    class Q10
    {
        static void Main()
        {
            try
            {
                Console.Write("Enter an alphabet:");
                string input = Console.ReadLine().ToLower();

                // Check whether the string is null or empty or not a single character
                if (string.IsNullOrEmpty(input) || input.Length != 1)
                {
                    Console.WriteLine("Please enter a single alphabet character.");
                    return;
                }

                char ch = Convert.ToChar(input);

                // Switch block to handle vowels and consonants
                switch (ch)
                {
                    case 'a':
                    case 'e':
                    case 'i':
                    case 'o':
                    case 'u':
                        Console.WriteLine("The alphabet is a vowel.");
                        break;
                    default:
                        if (char.IsLetter(ch))
                        {
                            Console.WriteLine("The alphabet is a consonant.");
                        }
                        else
                        {
                            Console.WriteLine("Invalid input. Please enter an alphabet character.");
                        }
                        break;
                }
            }
            catch (Exception ex)
            {
                Console.WriteLine("Something went wrong: " + ex.Message);
            }
            Console.ReadKey();
        }
    }*/
}
